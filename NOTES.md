# Notes

* `Site` model
    - Many urls (JSON column)
    - Many `Check`
    - `name`
    - `frequency` - 5 minutes
    - `lastCheck` one of many

* Scheduled job that runs every five minutes
* Tests
